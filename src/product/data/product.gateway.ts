export default class ProductGateway {
  // static async list(): Promise<Product[]> {
  //   const produtos = await ProductData.list()
  //   return produtos.map(produto2Product)
  // }
  // static async searchById(id?: string): Promise<Product> {
  //   const filter: string = id ? `id=${id}` : ''
  //   const produto = await ProductData.list(filter)
  //   return produto2Product(produto[0])
  // }
}
