// const productStoreKey = 'product'

export default class ProductService {
  // const { setStored, stored } = useLocalStorage<Product>(productStoreKey)
  // static list(): Promise<Product[]> {
  //   return ProductGateway.list()
  // }
  // static searchById(productId: string): Promise<Product> {
  //   return ProductGateway.searchById(productId)
  // }
  // static store(product: Product) {
  //   setStore(product)
  // }
}
